# AeroSLS Multi-Tenant Isolation Gap Analysis v0.1

## 0. Why this doc exists

This is a scoping document, not a findings-from-execution document like the LPAR or Multi-Node roadmaps' own "Findings addendum" sections — it exists to answer one question honestly before any architecture gets committed to paper: **if AeroSLS leased address space and services to real, paying customers today, exactly where would tenant A's workload touch tenant B's, and what would it cost to close each gap.** Every claim below is checked against the code and the existing roadmap docs as they stand today (`AeroSLS-LPAR-Roadmap-v0.1.md`, `AeroSLS-Multi-Node-Partition-Scaling-Roadmap-v0.1.md`, `AeroSLS-Database-Gap-Analysis-v0.1.md`, plus direct reads of `kernel/partition.c`, `kernel/database.c`, `kernel/frame_pool.c`, `kernel/auth.c`, `net/http.c`, `kernel/group_profile.c`/`authlist.c`/`security_audit.c`, `kernel/stream.h`, `kernel/tier_mgr.c`, `kernel/persist.c`), not recalled from memory.

**The short answer up front:** AeroSLS has real, well-tested, single-node multi-tenant isolation for four things — catalog/object access, process spawn, IPC, and CPU scheduling fairness — plus a partial, honestly-scoped start on cross-node placement. It has **no** concept of a tenant as a single provisionable entity, **no** storage-level isolation, **no** network-level isolation, **no** usage metering, and **no** tenant-scoped administration. Everything built so far answers "can tenant A's *code* reach tenant B's *objects*" — nothing yet answers "can I sell tenant A a guaranteed slice of storage, bandwidth, or admin control that tenant B cannot touch or see, and bill for it."

## 1. Inventory: what real isolation already exists

Two build efforts already exist and are both marked done. Neither was designed with billing/leasing in mind, but both are real, load-bearing primitives worth building on rather than replacing.

### 1.1 `partition_id` — resource and execution isolation, single node (LPAR Phases 8–14, all done)

A `partition_id` tag threaded through the choke points that already existed for something else, exactly the same pattern each time: `catalog_check_access()` gates object access, `process_create()`/`program_spawn()` gate process spawn, `ipc_user_send()`/`ipc_user_recv()` gate IPC ports, `schedule_ring3()`'s two-level search gates CPU turns, `frame_pool.c`'s accounted allocator gates physical RAM for 5 of 20 real allocation call sites. All host-tested with real execution, not just compile-check, and all real, caught bugs (Phase 12's scheduler starvation bug, the missing frame-free primitive in Phase 14) along the way.

**What this buys a leasing story:** a tenant assigned to partition *P* cannot read/write another partition's catalog objects, cannot spawn processes into another partition, cannot send/receive on another partition's bound IPC ports, and cannot starve another partition of every CPU turn. That's a real, defensible boundary for co-tenants sharing one machine.

**What it does not buy:** everything in §3 below.

### 1.2 `database_id` — SQL/catalog namespace and grants (Database Namespace & Access Roadmap, Phases 1–5, done; gap-closed further in the Database Gap Analysis)

A separate tag, on the same `object_catalog[]` entries, resolved through `database_check_access()` as an additive extension of `catalog_check_access()`. `CREATE DATABASE`, `GRANT`/`REVOKE` per database, `ALTER TABLE ... SET DATABASE`, cascading `DROP DATABASE ... CASCADE` — a real SQL-shaped multi-schema/multi-tenant namespace, independently gated.

**What this buys:** two SQL tenants sharing one AeroSLS instance can each have their own `CREATE DATABASE mycompany` namespace with independent grants, invisible to each other via `SHOW`/`SELECT` unless explicitly granted.

### 1.3 Cross-node scaling and cold migration (Multi-Node Partition Scaling Roadmap, Phases 1–6, done)

Real node identity/roster (`cluster_roster[]`), per-partition node ownership (`partition_owner_table[]`), per-partition Raft-lite write leases (`partition_lease_table[]`), partition-aware DSPP packet routing (`partition_id` added to the wire header, with a magic-value version bump), and `partition_migrate()` orchestration (pause → step down lease → reassign ownership → reclaim source frames).

**What this buys:** the bookkeeping for "which physical node currently owns tenant X's partition" is real, tested, and persisted. That's a genuine prerequisite for a dedicated-node tier.

**What this does not buy, named explicitly by the roadmap's own findings, twice:** *no actual page data moves.* `partition_migrate()`'s own findings addendum says it plainly — "there is no function anywhere in `net/dspp.c` that moves or retags a catalog object's `partition_id`, because no object-to-physical-frame resolution plumbing exists to move." Migration today moves an ownership *record*, not a tenant's data. A customer told "we moved you to a dedicated node" would, on the current codebase, have every object still physically sitting on the source machine's NVMe and RAM, just logically marked as belonging elsewhere and refusing to serve requests locally. This is the single largest gap standing between what's built and an actual dedicated-node product.

### 1.4 Authentication (Architectural Phase 4, Operational Phase E)

Bearer tokens (`struct LeaseToken`: email, uid, role, TTL) validated on every `/api/*` route except `/api/health`/`/auth/token`/`/auth/verify`, real credential check on issuance (password-derived key, not a bare-email handout), CORS locked down, TLS via a reverse proxy. This is real and already closes a whole class of "anyone can mint a token for any account" bugs the Architectural roadmap found and fixed.

## 2. The core architectural gap: tenancy is not one thing, it's two unconnected things

This is the most important finding in this document, and it did not require deep investigation to surface — a single grep proves it: `kernel/database.c` never references `partition_id`, anywhere, in either direction. `kernel/partition.c` never references `database_id`. They are two completely independent tagging systems living on the exact same `object_catalog[]` array, built by two different roadmaps that never introduced themselves to each other.

Concretely, today there is no such thing as "provision tenant Acme Corp." There are two separate, manual, uncoordinated procedures that happen to both use the word "isolation":

- `partition create acme` + `partition assign uid <n> acme` — gives Acme's processes/frames/IPC/CPU-turns a resource boundary, says nothing about SQL schema visibility.
- `CREATE DATABASE acme` + `GRANT ... IN DATABASE acme` — gives Acme's SQL objects a namespace boundary, says nothing about resource isolation. A uid granted access to database `acme` but never assigned to partition `acme` gets full SQL namespace isolation and zero resource isolation from every other tenant on the box, and vice versa.

Nothing stops these two axes from being assigned inconsistently for the same real-world customer (uid 500 could be in partition `acme` but granted into database `contoso`), because nothing today treats "tenant" as a single entity with both a partition and a database that must move together. Any future "tenant" concept needs to either unify these two axes under one identity or make the relationship between them an explicit, enforced invariant — right now it's neither; it's two coincidentally-parallel systems.

**[Closed for newly-provisioned tenants going forward — see §9.]** `tenant_create()` (`kernel/tenant.c`) now exists as the single "provision tenant Acme Corp" operation this section calls for. Pre-existing partitions/databases created via the old two-step manual path before this closure are unaffected and remain two independent, unlinked identities — this closes the gap for what gets provisioned from here on, not a retroactive migration of history.

## 3. What's shared vs. dedicated today, resource by resource

| Resource | Isolation today | Real gap for leasing |
|---|---|---|
| Catalog objects (tables, vector collections, streams-as-objects) | Real, enforced, tested (`catalog_check_access()` + `database_check_access()`) | None at the logical layer |
| Process spawn / execution | Real, enforced, tested (Phase 9) | None |
| IPC ports | Real, enforced, tested (Phase 11) | None |
| CPU scheduling | Starvation-prevention only, not proportional/weighted (Phase 12's own explicit scope cut) | Cannot sell "guaranteed 2 vCPU-equivalent" — every partition gets an equal round-robin slot regardless of what a customer paid for |
| Physical RAM frames | Accounted for exactly 5 of 20 real allocation call sites (process stacks/code, ELF segment loads); the other 15 — page-table internals, NVMe queues, SMP boot stacks, the shared SIMI activation cache, catalog index nodes, **and stream/blob storage** — are unaccounted, either correctly attributed to system overhead or (streams) genuinely untracked | A tenant uploading large blobs via the stream/blob store consumes real, unbounded, unquota'd physical memory today — `struct StreamEntry` (`kernel/stream.h`) carries no owner or partition field at all, confirmed by direct read, a gap LPAR Phase 13's own findings already named and left open |
| Physical frame *reclamation* | Real per-frame tracking exists for the accounted 5 sites (Multi-Node Phase 3 closed the LPAR Phase 14 leak) | Still bounded to those same 5 sites; frames from the other 15 (notably stream storage) are never reclaimed per-tenant, ever |
| NVMe/disk storage | **No partitioning at all.** `kernel/persist.c` writes every subsystem's data into one flat, shared LBA space; `kernel/frame_pool.c`'s bitmap is one shared free-list. There is no per-tenant NVMe region, quota, or byte accounting | A "your data lives on dedicated storage" tier is not physically true today under any tenant — everyone's bytes interleave in the same disk image |
| Storage tiering (hot/cold) | `kernel/tier_mgr.c` has zero partition/tenant awareness, confirmed by direct grep | One tenant's access pattern can promote/demote another tenant's data between tiers; no per-tenant tier SLA is possible today |
| Network / HTTP | **[Partially closed — see §8]** Per-partition HTTP request-rate limiting now real (`net/http_rate_limit.c`), gating all three HTTP auth chokepoints. `TCP_MAX_CONNS = 24` (`net/tcp.h`) is still one shared, cluster-wide, un-partitioned connection pool — no per-tenant connection quota exists | Request-level noisy-neighbor risk (one tenant flooding the server with requests) is closed. Connection-level risk (one tenant holding many long-lived connections) remains open — deliberately scoped out of this phase; see §8 for why |
| Cross-node placement | Real ownership bookkeeping (§1.3) | No real data movement (§1.3) — a dedicated-node tier cannot actually be fulfilled yet |
| Authentication / identity | Real bearer tokens, uid + role | uid is a flat, global namespace shared by every tenant; there is no tenant-scoped admin role (see §4) |
| RBAC (roles, groups, authorization lists) | `kernel/group_profile.c`, `kernel/authlist.c` — confirmed by grep to have **zero** `partition_id`/`database_id` awareness | A group or authorization-list entry is either global system-wide RBAC or nothing — there is no way to grant a customer's own staff an "admin within your partition only" role. Any uid with elevated RBAC authority has it everywhere, not scoped to one tenant |
| Security audit log | `kernel/security_audit.c` — same zero-awareness finding | Cannot hand a customer a filtered audit trail of "actions within your tenant only"; the log is one global stream |
| Billing / usage metering | **Does not exist.** Repo-wide search for billing/metering/usage-report/cost-tracking infrastructure returns nothing relevant — the only "quota" concept anywhere is the frame-count quota from LPAR Phase 13, which is an admission-control limit, not a metered, exportable usage record | Nothing today could generate an invoice. No subsystem records "partition X consumed Y frame-hours / Z requests / W storage-bytes over this billing period" |

## 4. Capacity ceilings that block real scale, independent of the isolation-completeness question

**[Closed — see §18]** Even if every gap above were closed, three compiled-in constants cap how many real customers this architecture could hold, all trivially raisable in isolation but each is also a signal that these tables were sized for a development sandbox, not a multi-tenant deployment, and a real leasing rollout needs to decide real numbers, not inherit these by accident. As of §18 these have been raised to a deliberate "medium ~256 tenant" target:

- `PARTITION_MAX` — was 16, now 256 (`kernel/partition.h`). The Multi-Node roadmap's own §1 already calls the old value "an arbitrary array size chosen to match `PROC_MAX`, trivially raised" — true, and it has now been raised to a real self-serve SaaS launch ceiling.
- `PARTITION_ASSIGN_MAX` — was 64, now 4096 — total `uid → partition` assignments, **across every tenant combined**, not per tenant (~16 users/tenant average across 256 tenants).
- `TCP_MAX_CONNS` — was 24, now 512 — already flagged in §3 as a shared-resource gap; it's also a hard scale ceiling independent of isolation. Raising it also surfaced and fixed a latent `uint8_t` truncation bug in `struct TCPConn.conn_id` (see §18).

None of these required new architecture to fix — they're `#define`s — but §18 documents everything else that had to move in lockstep (persisted-state LBA layout, several other tenant-count-keyed constants) and what was deliberately left as a disclosed trade-off (per-partition storage sub-range size).

## 5. What "leasing address space and services" actually requires that doesn't exist yet

Framed directly against the user's stated goal — leasing AeroSLS to real customers, shared vs. dedicated tiers, and scaling — here is the concrete, currently-missing list, ordered by how foundational each piece is to everything above it:

1. **[Closed — see §9] A single "tenant" identity that unifies `partition_id` and `database_id`.** Today provisioning a customer is two unrelated manual procedures (§2). A real product needs one `tenant_create()`-shaped operation that atomically creates both, and an invariant somewhere (probably a new small table, mirroring `partition_owner_table[]`'s own "separate table, not a bolted-on field" precedent) that keeps them consistent going forward.
2. **[Phase 1 closed, Phases 2–3 open — see `docs/AeroSLS-Storage-Isolation-Roadmap-v0.1.md` §6] Storage isolation.** Nothing physically separates tenant bytes on disk today (§3) — that remains true; Phase 1 closes the "unbounded, unquota'd" half of the finding (a combined per-partition rowstore+vecstore page quota now exists and is enforced), not the physical-separation half. This is still the largest, highest-risk gap for any tier that claims data isolation, let alone dedicated storage.
3. **[Closed — see §10] Real frame reclamation and quota coverage for stream/blob storage**, closing the one named-but-unclosed gap from LPAR Phase 13.
4. **[Partially closed — see §8]** Connection-level and request-level fairness at the network layer — per-tenant connection quotas and/or rate limiting, and `TCP_MAX_CONNS` sized for real concurrent tenancy, not today's shared 24. Request-level rate limiting is now real; connection-level quotas and the `TCP_MAX_CONNS` resize remain open.
5. **[Closed — see §11] Tenant-scoped RBAC.** A customer needs to be able to administer their own users without that authority leaking to (or being grantable over) another tenant's partition/database. `group_profile.c`/`authlist.c` need a `partition_id`/`database_id` scope dimension, the same shape Phase 3 of the security work already gave `catalog_check_access()` for database grants.
6. **[Closed — see §12] Usage metering.** A new subsystem, not an extension of an existing one — nothing today counts and exports per-partition consumption over time. This is a hard prerequisite for any usage-based billing, and a soft prerequisite even for flat-rate tiers (you still need to prove SLA compliance and detect abuse).
7. **[Closed — see §13] Real data movement for migration**, closing the gap named twice in the Multi-Node roadmap, before a dedicated-node tier can be sold honestly — "records move" is not "your data moved."
8. **[Closed — see §14] Weighted/proportional CPU scheduling**, if any tier wants to sell a CPU guarantee stronger than "won't be starved" (Phase 12's own explicit, honest scope cut). Capacity resizing, grouped under this same item, remains open — see item 9.
9. **[Closed — see §18] Deliberate capacity sizing** for `PARTITION_MAX`/`PARTITION_ASSIGN_MAX`/`TCP_MAX_CONNS` (and an audit for any other similarly-sized fixed table not surfaced by this pass) against a real target tenant count, not the current development defaults.

## 6. A first-cut tiering model this inventory naturally supports

Not a commitment, offered as a starting frame for the architecture conversation this doc exists to enable:

- **Shared tier** — multiple tenants per partition-set on one node, isolated by `partition_id` + `database_id` only (§1.1–1.2, already real). Cheapest to offer today, but honestly must be sold with the caveats in §3 disclosed (no storage isolation, no network fairness, no CPU guarantee) until those close.
- **Dedicated-partition tier** — one tenant per partition, still co-located on shared physical hardware with other tenants' partitions. Meaningfully stronger than shared once §5 items 2–5 close (storage isolation and network/RBAC fairness are what actually differentiate this from the shared tier today, since resource-level isolation is already real).
- **Dedicated-node tier** — one tenant, one physical machine, using Multi-Node's node-pinning and migration machinery. Cannot be sold honestly until §5 item 7 (real data movement) exists — right now "dedicated node" would be a true statement about ownership bookkeeping and a false statement about where the customer's bytes actually live.

## 7. Suggested priority if any of this gets picked up

1. **[Partially closed — see §8] Network fairness (§5.4)** — cheapest fix in this document (`TCP_MAX_CONNS` alone is a `#define`, though real per-tenant rate limiting is more work), highest immediate noisy-neighbor risk reduction, blocks nothing else.
2. **[Closed — see §9] Unify the tenant identity (§5.1)** — architecturally foundational; every other gap is easier to reason about and test once "tenant" is one thing instead of two coincidentally-parallel systems.
3. **[Closed — see §10] Stream/blob ownership + quota (§5.3)** — closes a already-named, already-understood gap (LPAR Phase 13's own findings scoped exactly what's missing); small, bounded, no new concepts needed.
4. **[Closed — see §11] Tenant-scoped RBAC (§5.5)** — needed before any tier can be self-service administered by the customer's own staff, which is close to a hard requirement for "real customers," not a nice-to-have.
5. **[Scoped — see `docs/AeroSLS-Storage-Isolation-Roadmap-v0.1.md`] Storage isolation (§5.2)** — large, foundational for any data-isolation claim; scoped into its own dedicated roadmap doc the way LPAR and Multi-Node each got one, given its size. Phase 1 of that doc (per-partition LBA quota accounting, mirroring LPAR Phase 13's frame quota exactly) is a concrete, immediately-implementable next step; real physical per-tenant LBA sub-ranges are deliberately deferred to a later phase, not attempted until a dedicated-storage tier is an actual product decision.
6. **[Closed — see §12] Usage metering (§5.6)** — can be built in parallel with the above once a stable tenant identity (item 2) exists to key metering records on; genuinely new subsystem, not an extension.
7. **[Closed — see §13] Real migration data movement (§5.7)** — gates the dedicated-node tier specifically; can be deferred until that tier is actually being sold, the same "don't build ahead of a concrete need" discipline the LPAR roadmap itself used to correctly close out nested partitions (§9 of that doc).
8. **[Closed — see §14] Weighted CPU scheduling (§5.8)** and **[Closed — see §18] capacity resizing (§5.9)** — lower urgency individually, but item 9 in particular should be revisited the moment any concrete target tenant count exists, since it's pure sizing work with no design risk.

## 8. Findings addendum: Network fairness, Phase 1 (§5 item 4 / §7 item 1) — partially closed

Implemented: per-partition HTTP request-rate fairness. New files `net/http_rate_limit.h`/`.c`, wired into all three existing auth chokepoints in `net/http.c` (GET/POST/DELETE route gates), each now returning HTTP 429 once a partition exhausts its window budget.

**Mechanism.** A fixed-window counter keyed on `partition_id` (resolved via `partition_get_for_uid(uid)`, matching every other multitenancy chokepoint in this codebase), not `uid` — so multiple uids in the same partition correctly share one budget rather than getting independent allowances. Window: 1000 ticks (~10s at the documented ~100Hz tick rate). Limit: 100 requests/window/partition, sized as roughly 8x headroom over the frontend's own documented background-polling baseline — a deliberately generous, tunable first cut, not a measured production value.

**Why request-rate, not connection-count.** Connection-level quotas were the other candidate mechanism this doc originally named. Two real properties of this server rule that out for now: `tcp_conns[]`/`TCP_MAX_CONNS` (`net/tcp.h`) is one small pool shared by both inbound tenant HTTP traffic and the kernel's own outbound connections (e.g. `net/ollama_client.c`), and `net/tcp.h`'s own comment already documents a real incident where inbound polling starved an outbound call out of that shared pool — slicing it per-tenant needs inbound/outbound separation first, a larger change not attempted here. Separately, `http_server_run()`'s own architecture serializes request dispatch (one request runs to completion before the next connection is serviced), so there's no "concurrent requests in flight per tenant" concept for a connection-level cap to even bound. Request-rate over time is the real, meaningful resource being protected.

**What's still open.** `TCP_MAX_CONNS = 24` remains one shared, un-partitioned, cluster-wide pool — connection-level noisy-neighbor risk (a tenant holding many long-lived connections) is not addressed by this phase, and neither is deliberate resizing of that constant against a real target tenant count (§5 item 9 / §7 item 8, unchanged).

**Verification.** `tests/http_rate_limit_host_test.c` — 6 scenarios (independent per-partition budgets, shared budget across uids in one partition, window reset timing, unassigned-uid fallback, out-of-range defensive guard), all real execution against the unmodified `net/http_rate_limit.c`, not a reimplementation. Full regression: `tests/run_all.sh`, 50/50 host tests passing, including this new one. `net/http.c` and `net/http_rate_limit.c` both compile clean under the real kernel cross-compile flags, no new warnings.

**Status:** §5 item 4 and §7 item 1 partially closed — request-level fairness done, connection-level fairness and capacity resizing remain, tracked under §5 item 9 / §7 item 8.

## 9. Findings addendum: unified tenant identity (§2 / §5 item 1 / §7 item 2) — closed

Implemented: `tenant_create()` (new files `kernel/tenant.h`/`.c`), the single operation this doc's §2 said didn't exist. Given a name and a caller uid, it atomically provisions both halves of a tenant — a `partition_create()`'d partition and a `database_create()`'d database — records the link in a new `tenants[]` table (`tenant_id` → `{partition_id, database_id, owner_uid}`, bump-allocated like `database_id`, never derived from either half so a `DROP`+recreate of one side never silently reattaches to stale state), and assigns the caller into their own new partition (skipped for uid 0/kernel, which is permanently `PARTITION_SYSTEM`).

**Ordering and rollback.** `partition_create()` runs first (cheaper to undo — `partition_destroy()` is real, well-exercised LPAR Phase 14 teardown), then `database_create()`. If the database step fails — most likely a name collision at the database layer — the partition just created is torn down via a real `partition_destroy()` call before `tenant_create()` returns, so a caller never observes an orphaned partition with no matching database. There is no cross-subsystem transaction primitive in this kernel; "atomic" here means an explicit, ordered sequence of calls into the already-existing, already-tested subsystems with an explicit rollback path, the same sense LPAR Phase 14 and Multi-Node Phase 6 already used the word.

**What this does and does not retroactively fix.** New tenants provisioned via `tenant_create()` from here forward have a real, enforced link between their partition and database. Partitions and databases created before this closure (or still created independently via the old two-step manual path, which remains available and unchanged) stay exactly as unlinked as §2 originally found them — this is a go-forward provisioning primitive, not a data migration or a restriction on the lower-level `partition_create()`/`database_create()` calls themselves.

**Reachability.** Syscalls `SYS_SLS_TENANT_CREATE`/`SYS_SLS_TENANT_LIST` (270-271, dispatched in `kernel/syscall_dispatch.c`), HTTP `GET`/`POST /api/tenants` (`net/http.c`, POST gated behind the existing auth chokepoint, `caller_uid` taken from the authenticated request never the body), and shell commands `tenant create <name>` / `tenant list` (`user/shell.c`). Persisted via `persist_tenants()` (`kernel/persist.h`/`.c`, new NVMe region at LBA 6784, same direct-restore shape as `persist_databases()`, `tenant_next_id` riding in the header for the identical reason `database_next_id` does).

**Verification.** `tests/tenant_host_test.c` — 8 scenarios (real partition_id/database_id linkage, duplicate-name rejection leaks nothing, uid-0/kernel exemption from self-assignment, non-kernel caller really lands in their own partition, both reverse lookups, database-collision rollback genuinely frees the partition slot for reuse, and partition-capacity exhaustion short-circuits before the database step ever runs), 28 checks, all real execution against the unmodified `kernel/tenant.c` linked with the real, unmodified `kernel/partition.c` and `kernel/database.c` — not a reimplementation of any of the three. Full regression: `tests/run_all.sh`, 51/51 host tests passing, including this new one (linking `kernel/tenant.c` newly required adding bare `tenants[]`/`tenant_next_id` storage declarations to the 24 existing host tests that already link the real `kernel/persist.c`, since `persist_restore_all()`/`persist_tenants()` now reference those globals — the same "declare the extern array directly" convention those files already use for `object_catalog[]` and others, not a functional change to any of them). All five touched/new kernel and net files compile clean under the real kernel cross-compile flags, zero new warnings.

**Status:** §2's core finding, §5 item 1, and §7 item 2 closed for newly-provisioned tenants.

## 10. Findings addendum: stream/blob ownership + quota (§5 item 3 / §7 item 3) — closed

Implemented: `struct StreamEntry` (`kernel/stream.h`) gained `owner_uid`/`partition_id` fields, stamped once at `stream_create()` time and never reassigned afterward — the same "owner fixed at creation" posture `object_catalog` entries already have. `stream_create()` now takes `caller_uid` as a real parameter (was previously anonymous/untagged) and resolves `partition_id` via the existing `partition_get_for_uid()`. This closes the exact gap LPAR Phase 13 itself named and left open: stream/blob frame allocation had no owner or partition concept at all, so it was invisible to the per-partition physical-frame quota system Phase 13 built for everything else.

**Frame accounting.** Every site in `kernel/stream.c` that allocates a physical RAM frame for stream data — `stream_write_chunk()`'s initial-write allocation and `stream_lazy_load_frame()`'s post-reboot lazy-reload allocation — switched from the old, plain, unaccounted `allocate_physical_ram_frame()` to Phase 13's own quota-checked `allocate_physical_ram_frame_for_partition(partition_id)`, the same entry point `process.c`/`loader.c` already use for process memory. A stream write against a partition at quota now fails cleanly (existing OOM-style error path, message text updated to say "OOM or quota exceeded") with no partial state change, instead of silently succeeding and consuming unaccounted physical memory.

**On-disk format.** The stream directory entry's on-disk layout (`dir_write_entry()`/`dir_read_entry()`) extended to persist `owner_uid`/`partition_id` at bytes 141-148 (padding shrinks from 141-447 to 149-447). Backward compatible: pre-existing persisted streams from before this change restore with `owner_uid=0`/`partition_id=0` (`PARTITION_SYSTEM`/default) because the old `dir_write_entry()` always zeroed the full entry before writing, not garbage — an honest, harmless default rather than a migration requirement. `stream_init()`'s restore-on-boot loop now correctly threads the persisted `owner_uid` through into the in-RAM catalog registration call, where it was previously hardcoded to 0 regardless of the real owner.

**Reachability.** `net/http.c`'s `api_stream_create()` now takes the authenticated request's `req_uid` and passes it through to `stream_create()`, the same "identity comes from the auth chokepoint, never the client-supplied body" convention already established for tenant creation (§9) and program-spawn (§7 item 6 / Phase F). `stream_list_json()` now includes `owner_uid`/`partition_id` in its JSON output so ownership is visible wherever the stream list is already surfaced.

**Verification.** `tests/stream_partition_host_test.c` — 5 scenarios (A: owner_uid/partition_id are genuinely the caller's, not left at 0; B: a real write increments the caller's partition frame usage by exactly 1; C: a second tenant's stream activity leaves the first tenant's usage completely unchanged; D: a 2-frame quota admits exactly 2 writes and cleanly denies a 3rd with usage still reading 2, no partial charge; E: an unassigned uid honestly falls back to `PARTITION_DEFAULT`, matching `partition_get_for_uid()`'s own contract), 15 checks, all real execution against the unmodified `kernel/stream.c` and `kernel/partition.c`.

The real `kernel/frame_pool.c` is deliberately *not* linked into this test. Its `alloc_raw_frame()` returns physical-address-shaped pointers (`frame_index × FRAME_SIZE`, e.g. `0x1000`) that are valid, writable memory once the real kernel identity-maps them, but are not valid mapped addresses in a host Linux process — and unlike the pre-existing `tests/frame_quota_host_test.c` (which only ever checks allocation success/failure and never dereferences the pointer), this test's entire point is to prove `stream.c` actually writes real bytes through the frames it allocates via the accounted path. The fix is a settable-fake `allocate_physical_ram_frame_for_partition()`/`partition_set_frame_quota()`/`partition_get_frame_usage()`/`partition_get_frame_quota()`, backed by real host `malloc()` and simple static per-partition counters replicating `frame_pool.c`'s real semantics exactly (0 = unlimited quota, denies cleanly without mutating usage once met/exceeded) — the same "fake the address-space-shaped primitive, keep the subsystem actually under test real" precedent already used elsewhere in this test suite. `frame_pool.c`'s own quota arithmetic remains separately, thoroughly verified for real by the existing, unmodified `tests/frame_quota_host_test.c`; this test's job is only to prove `stream.c` calls the accounted entry point with the right `partition_id` and correctly propagates a denial.

Full regression: `tests/run_all.sh`, 52/52 host tests passing, including this new one. All touched files (`kernel/stream.h`, `kernel/stream.c`, `net/http.c`) compile clean under the real kernel cross-compile flags, zero new warnings.

**Status:** §5 item 3 and §7 item 3 closed.

## 11. Findings addendum: tenant-scoped RBAC (§5 item 5 / §7 item 4) — closed

Implemented: `kernel/tenant.c` gained `tenant_caller_may_administer(caller_uid, partition_id)`, a shared permission gate now wired into `kernel/group_profile.c`'s `group_create()`/`group_add_member()` and `kernel/authlist.c`'s `authlist_create()`/`authlist_grant_object()`/`authlist_grant_uid()`/`authlist_grant_group()`. This closes a gap that was more serious on inspection than the original §5.5 framing suggested: those six functions previously took no `caller_uid` at all and had *zero* permission gate whatsoever — any uid, reachable via a bare `do_syscall()`, could mint a group naming any role (including `ROLE_DB_ADMIN`) or attach any grant to any object, with no identity check of any kind. `catalog_check_access()`'s own outermost partition boundary (`e->partition_id != partition_get_for_uid(uid)`) already prevented that unscoped state from being *exploitable* for cross-tenant object access, but it did nothing for the doc's actual ask: letting a tenant's own customer administer their own users without needing global authority, and keeping one tenant's admin actions from touching another tenant's names at all.

**The gate.** `tenant_caller_may_administer()` returns 1 for a `ROLE_SYSTEM_KERNEL` caller (mirrors `catalog_check_access()`'s own kernel-always-passes rule) unconditionally; for `PARTITION_SYSTEM` specifically (no single tenant owns it by definition) only a global `ROLE_DB_ADMIN` caller passes; otherwise the caller must be the exact `owner_uid` stamped on the `tenants[]` record for that partition — the same owner identity `tenant_create()` (§9) already records. A partition with no tenant record at all (created directly via the old `partition_create()` path) has no self-service owner and is denied outright, the honest answer rather than a silent fallback.

**Scoping mechanics.** `struct SLSGroupEntry`/`struct SLSAuthListEntry` each gained a `partition_id` field, stamped once at creation from the creating caller's own `partition_get_for_uid()` and never reassigned — the same "scope fixed at creation" posture `stream.c`'s `owner_uid`/`partition_id` (§10) already established. Beyond the create-time gate, every subsequent mutation enforces a same-partition invariant on what it attaches: `group_add_member()` refuses a member uid whose own partition doesn't match the group's; `authlist_grant_uid()` refuses a grantee uid the same way; `authlist_grant_group()` refuses a grantee group scoped to a different partition (new `group_get_partition_id()` accessor); `authlist_grant_object()` refuses attaching a grant on an object belonging to a different partition (direct `object_catalog[]` scan). Cross-tenant membership/grants are refused outright at the point of attachment, not silently permitted and left to `catalog_check_access()`'s boundary to make harmless after the fact.

**Reachability.** `SLSGroupCreateRequest`/`SLSGroupAddMemberRequest`/`SLSAuthListCreateRequest`/`SLSAuthListGrantRequest` each gained a `caller_uid` field; `user/shell.c`'s `group create`/`group add`/`authlist create`/`authlist grant obj|uid|group` commands now populate it from `current_session_uid`, the same convention already used for `database create`/`tenant create`. No HTTP routes exist for these six operations yet (confirmed via search — group/authlist administration has only ever been shell/syscall-reachable, unchanged by this phase); `group_contains_uid()`/`group_list()`/`authlist_check_access()`/`authlist_list()` are read-only introspection and authorization-check paths, left ungated exactly as before, matching `catalog_check_access()`'s own established call pattern.

**What this does and does not retroactively fix.** Neither `group_table[]` nor `authlist_table[]` is persisted to NVMe (confirmed via search of `persist.c`) — both are in-memory-only, so there is no pre-existing on-disk state to migrate or reconcile; every group/authlist created from here forward goes through the new gate unconditionally, with no legacy-compatibility carve-out needed.

**Verification.** New `tests/rbac_scope_host_test.c` — 9 scenarios (A: the recorded tenant owner administers their own tenant's RBAC; B: a same-tenant uid can be added as a member, a stranger cannot; C: neither a different tenant's owner nor a mere member of the tenant — as opposed to its recorded owner — can administer it; D: two tenants' RBAC stay fully independent; E: the `PARTITION_SYSTEM` special case — bare GUEST denied, global `ROLE_DB_ADMIN` and kernel both pass; F: `authlist_create()` gets the identical owner gate; G: `authlist_grant_object()` refuses a cross-tenant object, succeeds on same-tenant, fails cleanly on a nonexistent name; H: `authlist_grant_uid()`/`authlist_grant_group()` apply the same same-partition invariant to grantees; I: end-to-end sanity that a legitimately-scoped grant chain still actually grants access, and an ungranted uid is still denied), 27 checks, all real execution against the unmodified `kernel/tenant.c`, `kernel/partition.c`, `kernel/database.c`, `kernel/group_profile.c`, and `kernel/authlist.c` linked together — not a reimplementation of any of them. `catalog_get_role()` is a settable fake (real per-uid control needed to exercise both the kernel/global-admin bypass paths and the default-GUEST path); `object_catalog[]`/`object_catalog_count` are bare storage, the same "declare the extern array directly" convention `tenant_host_test.c`/`database_host_test.c` already established.

Fixing the six pre-existing host tests that link the real `kernel/group_profile.c`/`kernel/authlist.c` (`legacy_rowstore_boundary_host_test.c`, `database_grant_phase3_host_test.c`, `security_phase3_host_test.c`, `sql_ddl_phase5_host_test.c`, `sql_database_phase2_host_test.c`, `sql_schema_export_import_host_test.c`) required adding a permissive local fake `tenant_caller_may_administer()` (always returns 1) to each, since none of them link `kernel/tenant.c` and none of them are testing RBAC scoping themselves — the same judgment call already applied throughout this test suite for address-space-shaped or cross-subsystem primitives that are out of scope for a given file. The two of those six that call `group_create()`/`group_add_member()`/`authlist_create()`/`authlist_grant_*()` directly (`database_grant_phase3_host_test.c`, `security_phase3_host_test.c`) had their call sites updated to pass a `caller_uid` argument (harmless under the permissive fake).

Full regression: `tests/run_all.sh`, 53/53 host tests passing, including the new one. All touched files (`kernel/tenant.h`, `kernel/tenant.c`, `kernel/group_profile.h`, `kernel/group_profile.c`, `kernel/authlist.h`, `kernel/authlist.c`, `user/shell.c`) compile clean under the real kernel cross-compile flags, zero new warnings.

**Status:** §5 item 5 and §7 item 4 closed.

## 12. Findings addendum: usage metering (§5 item 6 / §7 item 6) — closed

Implemented: `kernel/usage_metering.h`/`.c`, a genuinely new subsystem (confirmed via search before writing it: no billing/metering/usage-report/cost-tracking infrastructure existed anywhere in the repo, matching the original §3 finding exactly). Rather than instrumenting every subsystem from scratch, this closes the gap by metering two resources that are already tracked *somewhere* but never accumulated over time: cumulative HTTP requests per partition, and "frame-ticks" — a GB-hours-style resource-time metric built by sampling LPAR Phase 13's existing `partition_get_frame_usage()` gauge (RAM frames currently held) against elapsed `kernel_tick_counter` time.

**Why these two, and not more.** `net/http_rate_limit.c` already resolves `partition_id` per authenticated request and counts it, but against a FIXED WINDOW that resets every `HTTP_PARTITION_RATE_WINDOW_TICKS` — good for admission control, useless for a running billing total, since the counter zeroes itself. `usage_metering_record_request()` is a second, independent, never-reset counter recording the same event. Frame-ticks turn a point-in-time gauge (current RAM usage, Phase 13) into a real accumulated usage record the same way a cloud biller turns "current instance size" into "GB-hours" — quantity × time held, summed every sample. Deliberately **not** metered in this phase: CPU time (no per-partition scheduling-tick counter exists anywhere — LPAR Phase 12 proved starvation-prevention fairness, not usage accounting) and disk bytes (belongs to the separately-scoped `docs/AeroSLS-Storage-Isolation-Roadmap-v0.1.md`, per the explicit instruction to address storage isolation separately from this work). A future phase adding either should extend `struct SLSUsageEntry` with the new field rather than build a second, parallel metering mechanism.

**Shape of the counters.** `usage_table[PARTITION_MAX]`, a flat array indexed directly by `partition_id` — the same "separate table keyed by partition_id, not a field bolted onto `struct SLSPartitionEntry`" shape `partition_owner_table[]` (Multi-Node Phase 2) and `frame_pool.c`'s own `partition_frame_usage[]`/`partition_frame_quota[]` arrays already established. Every counter is lifetime-cumulative-since-boot; a real billing-period boundary (reset at invoice-cycle start, or timestamped historical records) is real future work, deliberately not guessed at here — this phase's job was proving the counters themselves are real and accurate, not designing a billing cycle. `usage_metering_tick()` is internally gated to accumulate only every `USAGE_METER_TICK_GATE` (100) calls, mirroring `tier_mgr_tick()`'s own "keep AP overhead low" gating convention, just coarser since frame usage changes far less often than tier hot/cold access patterns. A partition's first-ever observation seeds `last_sample_tick` without accumulating (an `initialized` flag per entry) — otherwise a partition created long after boot would get charged a bogus frame-tick jump for time it never existed, the same caution `persist.c`'s magic-number-gated restores already apply elsewhere.

**Reachability.** New syscall `SYS_SLS_USAGE_REPORT` (272, confirmed as the next free number via a fresh grep of every `kernel/*.h SYS_SLS_*` define before picking it, per this codebase's own convention), dispatched in `kernel/syscall_dispatch.c` to `sys_sls_usage_report()`. `kernel/microkernel.c`'s AP service-poll loop calls `usage_metering_tick()` immediately alongside its existing `tier_mgr_tick()` call. `user/shell.c` gained a `usage report` command (`do_syscall(SYS_SLS_USAGE_REPORT, 0)`) plus help text. `net/http.c` gained `usage_metering_record_request()` calls at all three of its GET/POST/DELETE `/api/*` rate-limit chokepoints (right after each `http_partition_rate_check()` success, so accounting only ever records requests that were actually admitted) and a new `GET /api/usage` route returning a JSON array of every active partition's `partition_id`, `name`, `http_requests_total`, `frame_ticks_total`, and current `frames_now` (a live gauge, not part of the cumulative record, included for convenient side-by-side comparison).

**Verification.** New `tests/usage_metering_host_test.c` — 7 scenarios (1: HTTP request counting is cumulative and independent per partition; 2: a partition's first-ever sample seeds the clock and accumulates zero frame-ticks, not a bogus jump; 3: a real elapsed interval accumulates exactly `frame_usage × elapsed_ticks`; 4: a mid-stream frame-usage change is applied only to the interval after it took effect, never retroactively; 5: an inactive partition is never sampled regardless of its fake frame usage or request count; 6: out-of-range `partition_id` reads/records are safe no-ops, mirroring `frame_pool.c`'s own never-fails posture; 7: the tick gate is a real gate — fewer than `USAGE_METER_TICK_GATE` calls never triggers a sample no matter how much simulated time elapsed, and the Nth call that crosses the gate finally samples the full accumulated jump), 16 checks, all real execution against the unmodified `kernel/usage_metering.c` — not a reimplementation. `kernel_tick_counter` is a bare settable global (driven forward manually to simulate elapsed time, the same "drive the real counter directly" approach `frame_quota_host_test.c` used for allocation counts); `partition_get_frame_usage()` and `partition_get_for_uid()` are small settable fakes; `partition_table[]` is bare storage (`kernel/partition.c` itself not linked, since `usage_metering.c`'s only real dependency on it is the table's shape and the `active`/`partition_id`/`name` fields it reads directly).

Full regression: `tests/run_all.sh`, 54/54 host tests passing (53 pre-existing + this phase's new one), zero regressions. All touched/new files (`kernel/usage_metering.h`, `kernel/usage_metering.c`, `kernel/syscall_dispatch.c`, `kernel/microkernel.c`, `user/shell.c`, `net/http.c`) compile clean under the real kernel cross-compile flags (`gcc -ffreestanding -fno-stack-protector -fno-pic -m32 -I . -I kernel -I drivers -I user -fsyntax-only`) — only the pre-existing freestanding-environment implicit-declaration warnings this codebase already carries everywhere (`kernel_serial_print`/`strcmp`/etc. resolve only through the full kernel include chain, not a syntax-only single-file check), no new warnings introduced by this phase's changes. `Makefile`'s `X86_C_SRC` list updated with `kernel/usage_metering.c`.

**Status:** §5 item 6 and §7 item 6 closed. Storage isolation (§5 item 2 / §7 item 5) remains scoped-only per the earlier addendum and the user's explicit instruction to address it separately; real migration data movement (§7 item 7) and weighted CPU scheduling / capacity resizing (§7 item 8) remain the two doc-recommended deferrals, unchanged.

## 13. Findings addendum: real migration data movement (§5 item 7 / §7 item 7) — closed

Implemented: `kernel/stream.c`'s new `stream_relocate_partition(partition_id, dest_node_id)`, wired into `kernel/partition.c`'s `partition_migrate()` as a genuine new Step 3, replacing the "deliberately NOT called here" disclaimer the Multi-Node Partition Scaling Roadmap's own Phase 6 findings used twice ("This function does not copy or move any object data"; "'migration' here means the ownership record moves, not that any data is copied"). This is the real byte-copy step that never existed before this phase.

**A prerequisite finding that reshaped the scope.** Before writing any code, investigation confirmed AeroSLS is genuinely single-node in every real deployment: `cluster_init()` is never called from any real boot path (only host tests call it), there is no receive-side dispatcher anywhere in `net/dspp.c` for any inter-node opcode, and there is exactly one shared NVMe image (`sls_storage.img`) with no second node's storage anywhere in the codebase. "Real data movement between nodes" therefore has no literal second machine to move bytes onto today. Rather than defer on that basis, or build a hollow wire-protocol stub with nothing real on the receiving end, the honest, immediately useful scope chosen (confirmed with the user) was: a genuine relocate-and-verify primitive that physically copies a partition's data to a fresh location and verifies it byte-for-byte, the same copy step a future real cross-machine transport would still need to run on its sending side before putting bytes on a wire this codebase does not have yet. "Destination" in `stream_relocate_partition()` therefore means a fresh slot within the shared `STREAM_MAX` pool, not a different machine — named explicitly in both the function's header comment and this addendum, not glossed over.

**Why scoped to streams only.** Streams/blobs already carry a definite `owner_uid`/`partition_id` per slot (§10 of this doc) with self-contained, individually relocatable 64 MiB byte ranges. `rowstore.c`/`vecstore.c` table pages have no per-partition page indexing at all yet — that is `docs/AeroSLS-Storage-Isolation-Roadmap-v0.1.md` Phase 1's job, not yet built, and a real prerequisite dependency rather than an oversight here. `kernel/partition.c`'s Step 3 comment now names this explicitly: rowstore/vecstore data still only moves in the ownership-record sense until Storage Isolation Phase 1 lands.

**The mechanism.** For every active stream slot owned by `partition_id`, `stream_relocate_partition()` finds a free destination slot, copies each populated page (up to `frames_used`) from the source LBA range to the destination LBA range via a 4 KiB staging buffer, reads the destination page back, and byte-compares it against what was read from the source before proceeding to the next page. Only after every page verifies clean does the destination slot take over the stream's identity (name, mime type, size, owner_uid, partition_id) and the source slot get retired (zeroed, deactivated). A copy/verify failure at any page aborts the whole slot's relocation, leaving the source fully intact and returning an honest partial count rather than a corrupted or half-moved state. Running out of free destination slots is handled the same way. `stream_persist_directory()` is called once at the end if anything relocated, so the new layout survives a reboot.

**A real bug the host test caught before implementation was accepted.** The first working version had a genuine defect: relocating slot *i* to a later slot *dst* left the destination active with the same `partition_id`, so the outer scan could reach `dst` later in the same call and "relocate" it again — in the worst case ping-ponging a stream back into its own original slot while inflating the reported relocated count. `tests/migration_data_movement_host_test.c` caught this directly (an "exactly 1 relocated" check failed, returning 2; the "different slot" check failed, showing the stream back where it started). The fix is a per-call `already_dst[]` flag array marking any slot just used as a destination as ineligible to be treated as a fresh source for the remainder of that call — documented in `stream.c` alongside the fix so the failure mode this guards against isn't silently lost.

**Reachability.** No new syscall or shell surface was needed — `stream_relocate_partition()` is called internally by `partition_migrate()`, already reachable via `SYS_SLS_PARTITION_MIGRATE` (218) and the existing `partition migrate <id> <dest_node>` shell command. `partition_migrate()`'s closing log line now reports the stream relocation count alongside the existing lease/frame-reclamation summary.

**What this does and does not close.** Stream/blob data genuinely, physically relocates and is verified byte-for-byte — a real fix, not a records-only one, for that one subsystem. Catalog objects backing rowstore/vecstore tables still keep their existing `object_id`/`partition_id` completely unchanged by a `migrate()` call, exactly as before this phase; a future phase extending real relocation to table data depends on Storage Isolation Roadmap Phase 1 first giving rowstore/vecstore a per-partition page index to walk. Real cross-machine transport (an actual DSPP wire protocol with a receive-side dispatcher, and real per-node storage) remains unbuilt and is the genuine remaining prerequisite for "your data moved to a different machine" to be literally true — named honestly here rather than implied by this phase's real, but same-disk, relocation work.

**Verification.** New `tests/migration_data_movement_host_test.c` — 5 scenarios (A: a real byte-for-byte relocation of a single-frame stream, verified both by reading the new NVMe location directly and by lazy-reloading through the normal `stream_lazy_load_frame()` API; B: a multi-frame stream relocates every page, in order, each individually verified; C: relocating one partition's streams never touches another partition's already-relocated streams; D: a forced NVMe write failure leaves the source fully intact and reports an honest zero relocated, not a corrupted partial state; E: an out-of-free-slots condition is handled the same honest way), 28 checks, all real execution against the unmodified `kernel/stream.c` and `kernel/partition.c` — not a reimplementation of either. Uses a stateful fake NVMe (a small in-memory LBA-indexed page store, since this test's whole point is proving actual bytes survive a round trip, unlike `stream_partition_host_test.c`'s trivial always-fail/always-succeed stubs which never needed to). `partition_migrate_phase6_host_test.c` and 8 other pre-existing tests that link the real `kernel/partition.c` (`dspp_phase5_host_test.c`, `ipc_partition_host_test.c`, `partition_host_test.c`, `persist_partition_host_test.c`, `rbac_scope_host_test.c`, `scheduler_fairness_host_test.c`, `tenant_host_test.c`, `workmgmt_phase4_host_test.c`) each got a permissive local fake `stream_relocate_partition()` (always returns 0, "nothing to relocate") added, since none of them link the real `kernel/stream.c` and none of them are testing stream relocation themselves — the same judgment call already applied throughout this suite for out-of-scope cross-subsystem primitives.

Full regression: `tests/run_all.sh`, 55/55 host tests passing (54 pre-existing + this phase's new one), zero regressions. All touched files (`kernel/stream.h`, `kernel/stream.c`, `kernel/partition.c`) compile clean under the real kernel cross-compile flags, zero new warnings.

**Status:** §5 item 7 and §7 item 7 closed (scoped to stream/blob storage; rowstore/vecstore data movement remains dependent on Storage Isolation Roadmap Phase 1). Weighted CPU scheduling / capacity resizing (§7 item 8) remains the one doc-recommended item not yet addressed. Storage isolation (§5 item 2 / §7 item 5) remains scoped-only per the earlier addendum, to be addressed separately per the user's instruction.

## 14. Findings addendum: weighted CPU scheduling (§5 item 8 / §7 item 8) — closed (capacity resizing remains open)

Implemented: a burst-style weighted round robin, extending `kernel/process.c`'s existing `pick_next_partition()` (LPAR Phase 12's partition-fair scheduler) rather than replacing it. `partition_set_cpu_weight(partition_id, weight)` / `partition_get_cpu_weight(partition_id)` record an explicit per-partition weight in a new `partition_cpu_weight[PARTITION_MAX]` table (0/unset reads back as the default weight of 1 via `effective_cpu_weight()`, the same "0 == default" convention LPAR Phase 13's frame quota already established).

**Mechanism.** Before rotating to the next candidate partition, `pick_next_partition()` now first checks whether the partition it just scheduled (`last_partition`) is still owed consecutive turns under its weight (`g_partition_turns_remaining > 0`) and still has a runnable process — if so, it gets another turn immediately instead of rotating. When the normal rotation loop does pick a new candidate, `g_partition_turns_remaining` is set to `effective_cpu_weight(candidate) - 1` (the turn just granted counts as the first of the weight). A partition with weight 3 therefore receives 3 consecutive turns per round before rotation resumes, proportional to its configured weight rather than Phase 12's flat "one turn per partition per round."

**Backward compatibility, by construction not just by argument.** At the default weight of 1 everywhere (every partition today, until an operator explicitly configures otherwise), `effective_cpu_weight(candidate) - 1` is always `0`, so `g_partition_turns_remaining` can never become nonzero, so the new "reuse `last_partition`" branch can never fire — the function reduces byte-for-byte to Phase 12's original round robin. This was verified directly, not just argued: Scenario 9 of the updated host test re-runs Scenario 1's exact flow and additionally asserts `g_partition_turns_remaining == 0` throughout, and all of Phase 12's original 80 checks (Scenarios 1–7) pass completely unmodified.

**Reachability.** New syscalls `SYS_SLS_PARTITION_CPU_WEIGHT_SET`/`SYS_SLS_PARTITION_CPU_WEIGHT_LIST` (273–274, confirmed as the next free numbers via a fresh grep before picking them, per this codebase's own convention), dispatched in `kernel/syscall_dispatch.c`. `user/shell.c` gained `partition cpuweight set <id> <weight>` / `partition cpuweights` commands plus help text, mirroring the existing `partition quota`/`partition quotas` pair's shape exactly. `net/http.c` gained `GET /api/partition/cpuweights` (lists every partition with a non-default weight) and `POST /api/partition/cpuweight` (body `{"partition_id": N, "weight": N}`), mirroring the existing frame-quota HTTP pair's handler structure.

**Verification.** Extended the existing `tests/scheduler_fairness_host_test.c` (rather than adding a new file, since it already links the real, unmodified `kernel/process.c` via the established `#include "kernel/process.c"` technique) with two new scenarios: Scenario 8 (a partition at weight 3 receives exactly 3x a sibling partition's turns at weight 1, deterministically, over whole rounds — `partition_set_cpu_weight()`/`partition_get_cpu_weight()` also checked for the 0-resets-to-default convention and safe out-of-range handling) and Scenario 9 (the backward-compatibility guarantee above, verified directly against the internal `g_partition_turns_remaining` state, not just inferred from outcomes). 15 new checks, all passing, all real execution against the unmodified `kernel/process.c` and `kernel/partition.c`. Full regression: `tests/run_all.sh`, 55/55 host tests passing (the count is unchanged from §13 since this phase extended an existing file rather than adding a new one), zero regressions. All touched files (`kernel/process.h`, `kernel/process.c`, `kernel/syscall_dispatch.c`, `user/shell.c`, `net/http.c`) compile clean under the real kernel cross-compile flags, zero new warnings beyond this codebase's pre-existing freestanding-environment implicit-declaration set.

**What this does not close.** Capacity resizing (`PARTITION_MAX`/`PARTITION_ASSIGN_MAX`/`TCP_MAX_CONNS`), grouped alongside CPU scheduling under §5 item 8/§7 item 8's original framing, remains open — it is pure `#define` sizing work that the doc has consistently held should wait for a real target tenant count (a business input, not a code gap) rather than be guessed at here.

**Status:** §5 item 8 and §7 item 8 closed for weighted CPU scheduling specifically. Capacity resizing (§5 item 9) remains the one open item in this document's original priority list. Storage isolation (§5 item 2 / §7 item 5) — see §15/§16 below for Phase 1 and Phase 2's closure; Phase 3 of that roadmap remains open.

## 15. Findings addendum: storage isolation, Phase 1 (§5 item 2 / §7 item 5) — Phase 1 closed, Phases 2–3 open

Implemented: `docs/AeroSLS-Storage-Isolation-Roadmap-v0.1.md` Phase 1 — a combined per-partition on-disk page quota (`kernel/storage_quota.h`/`.c`) shared by `rowstore.c` and `vecstore.c` (both use an identical 4 KiB page size, so one counter covers both rather than two parallel tables), wired into both subsystems' real page-allocating call sites (`rowstore_alloc_page()`, `vecstore_alloc_page()`) with denial happening before the shared bump-allocator cursor advances or any RAM frame is touched. Stream storage was deliberately left out of this phase — `STREAM_MAX` (8) fixed slots is already an adequate system-wide ceiling on its own, a decision the roadmap doc's own §5 open questions explicitly called for and §6 of that doc now resolves.

**What this changes about §3's original finding.** The §3 table's "NVMe/disk storage" row said plainly: "No partitioning at all... There is no per-tenant NVMe region, quota, or byte accounting." The "quota, or byte accounting" half of that is now false — a real, tested, enforced per-partition page budget exists for the two subsystems that account for essentially all real growable table/collection data. The "No partitioning at all... per-tenant NVMe region" half remains exactly as true as it was: a partition's pages are still scattered arbitrarily within the shared LBA pools, never contiguous or reserved. This phase is accounting, not physical separation — the same "bolted onto an unchanged shared pool" posture LPAR Phase 13's frame quota already established for RAM, extended to disk.

**Verification.** New `tests/storage_quota_host_test.c` (9 scenarios, 45 checks) plus new scenarios in `tests/rowstore_host_test.c` and `tests/vecstore_host_test.c` proving each subsystem's real call site threads the right `partition_id` through and denies cleanly at quota. Full detail, mechanism, and the two resolved open-question decisions are written up in `docs/AeroSLS-Storage-Isolation-Roadmap-v0.1.md` §6, not duplicated here. Full regression: `tests/run_all.sh`, 56/56 host tests passing, zero regressions.

**Status:** Storage Isolation Roadmap Phases 1, 2, and 3 are all now closed — see §17 for Phase 3. §5 item 2 / §7 item 5 are closed.

## 16. Findings addendum: storage isolation, Phase 2 (§5 item 2 / §7 item 5) — byte-level usage export closed

Implemented: `docs/AeroSLS-Storage-Isolation-Roadmap-v0.1.md` Phase 2 — Phase 1's real per-partition page counters (`kernel/storage_quota.c`) are now exposed as bytes, per-partition, on the existing disk-status reporting path: `kernel/tier_mgr.c`'s `sys_sls_disk_status()` gained a new printed section, and `net/http.c`'s `GET /api/disk` (`api_disk_json()`) gained a matching `"partitions"` JSON array (`partition_id`/`disk_bytes_used`/`disk_bytes_quota`). No new syscall, no new persisted state: `usage_metering.h`'s `struct SLSUsageEntry` was deliberately left untouched, since `storage_quota.c` is already the single source of truth for this data as of Phase 1 — mirroring it into a second array would itself be the "second, parallel metering mechanism" that struct's own header comment warns against. Full mechanism and the design-fork writeup live in `docs/AeroSLS-Storage-Isolation-Roadmap-v0.1.md` §7, not duplicated here.

**Verification.** `tests/tier_capacity_phase5b_host_test.c` extended with a new scenario proving the real `sys_sls_disk_status()` runs to completion against real `storage_quota.c` data (a smoke test — no serial-capture mechanism exists in this suite to assert printed byte values directly). `net/http.c` remains too heavy to host-test (per `tests/http_rate_limit_host_test.c`'s own established rationale); the new JSON code was verified via a clean `gcc -fsyntax-only` pass instead. Full regression: `tests/run_all.sh`, 56/56 host tests passing, zero regressions.

**Status:** Storage Isolation Roadmap Phase 2 closed. Phase 3 closed too — see §17.

## 17. Findings addendum: storage isolation, Phase 3 (§5 item 2 / §7 item 5) — real per-partition page sub-ranges closed

A dedicated-storage tier became an actual product decision, ending Phase 3's design-only deferral. Implemented: `docs/AeroSLS-Storage-Isolation-Roadmap-v0.1.md` Phase 3 — the rowstore/vecstore page_id space is now split into `PARTITION_MAX` fixed, contiguous, per-partition sub-ranges (`kernel/rowstore.c`'s `rowstore_partition_cursor[]`, `kernel/vecstore.c`'s `vecstore_partition_cursor[]`), so a partition's on-disk pages are physically confined to its own reserved slice rather than merely accounted for within a shared pool. This is the change that finally makes §3's original finding — *"No partitioning at all... There is no per-tenant NVMe region"* — false rather than merely mitigated: a per-tenant NVMe region now genuinely exists for rowstore and vecstore data.

**What changes about §3/§15's own prior finding.** §15 (Phase 1's own addendum) said plainly: "The 'No partitioning at all... per-tenant NVMe region' half remains exactly as true as it was: a partition's pages are still scattered arbitrarily within the shared LBA pools, never contiguous or reserved." That is now closed. A partition's rowstore/vecstore pages are contiguous-by-construction within its own reserved sub-range, never interleaved with any other partition's.

**Persistence and quota interaction.** Two new small persisted arrays (`PERSIST_ROWSTORE_PARTCURSOR_LBA`/`PERSIST_VECSTORE_PARTCURSOR_LBA`) carry each partition's own cursor across reboot, gated by a one-way format-version marker so a pre-Phase-3 snapshot correctly falls back to cold-start sub-range defaults rather than trusting stale/absent data. Storage Isolation Phase 1's configurable page_quota now sits beneath a second, hard, physical ceiling (the sub-range size itself) — a quota configured above that physical capacity simply never binds. Full detail in `docs/AeroSLS-Storage-Isolation-Roadmap-v0.1.md` §8, not duplicated here.

**Verification.** New scenarios in `tests/rowstore_host_test.c` and `tests/vecstore_host_test.c` prove disjoint per-partition page ranges, independent exhaustion (one partition's slice can be full while a different partition allocates normally), and persistence round-trip correctness for the new per-partition cursor arrays. Full regression: `tests/run_all.sh`, 56/56 host tests passing, zero regressions.

**Status:** Storage Isolation Roadmap is now fully closed (Phases 1, 2, and 3). §5 item 2 / §7 item 5 are closed.

## 18. Findings addendum: deliberate capacity sizing (§4 / §5 item 9 / §7 item 8) — closed

§4's own framing was explicit: these constants were development-sandbox defaults, and "a real leasing rollout needs to decide real numbers, not inherit these by accident." A concrete target — "medium," roughly 256 tenants — is now that real number, and every constant that would otherwise have silently become the new binding ceiling, or stayed an under-scaled independent mirror, was raised in lockstep rather than just the three named in §4.

**Constants changed.**

- `PARTITION_MAX` (`kernel/partition.h`): 16 → 256.
- `PARTITION_ASSIGN_MAX` (`kernel/partition.h`): 64 → 4096 (~16 users/tenant average across 256 tenants).
- `TCP_MAX_CONNS` (`net/tcp.h`): 24 → 512.
- `PARTITION_LEASE_MAX` (`net/consensus.h`): 16 → 256 — a linear-scan fail-closed lease pool, not indexed by `partition_id`, that would otherwise silently cap write-lease-capable partitions at 16 regardless of `PARTITION_MAX`.
- `TENANT_MAX` (`kernel/tenant.h`): 32 → 256 — `tenant_create()` is the real go-forward provisioning path (§9) and would otherwise become the silent binding ceiling on tenant count even after `PARTITION_MAX` was raised.
- `DATABASE_MAX` (`kernel/database.h`): 32 → 256, and `DATABASE_GRANT_MAX`: 64 → 1024 (same reasoning as `TENANT_MAX`; `DATABASE_GRANT_MAX_UIDS`/`DATABASE_GRANT_MAX_GROUPS`, per-grant sub-limits unrelated to tenant count, were left unchanged).

**A latent bug the resize surfaced, not caused.** `struct TCPConn.conn_id` (`net/tcp.h`) was `uint8_t`, which would have silently wrapped modulo 256 once `TCP_MAX_CONNS` exceeded 256. Widened to `uint16_t` (`net/tcp.h`, `net/tcp.c`'s `tcp_init()`). Nothing currently reads this field back for lookup, but it was a real landmine left for whenever something eventually does.

**The corruption risk this pass caught before it shipped.** `kernel/persist.c`'s `persist_write_array()`/`persist_read_array()` have no bounds or overlap check of their own — they walk forward from a given LBA trusting the caller. `kernel/persist.h`'s LBA layout had, in several places, exactly zero slack between the end of one region and the next region's header. Raising `PARTITION_MAX`/`PARTITION_ASSIGN_MAX`/`DATABASE_MAX`/`DATABASE_GRANT_MAX`/`TENANT_MAX` grows `partition_table[]`, `partition_assign_table[]`, `databases[]`, `database_grants[]`, and `tenants[]` well past their old 1-frame allotments — without remediation, a grown region's write would have silently overwritten the next region's still-in-flight data, corrupting unrelated subsystems' persisted state on the very first boot after the resize. `kernel/persist.h`'s entire LBA layout from `PERSIST_PART_HDR_LBA` onward was rewritten with exact `sizeof()`-verified frame counts and a deliberate 1-frame safety gap inserted after every region, so a future resize of any one constant can no longer silently corrupt its neighbor. Final layout ends at LBA 7568, comfortably clear of `STREAM_DIR_LBA` (8192) with 78 frames of margin. Regions before `PERSIST_PART_HDR_LBA` (catalog/records/schemas/programs) are untouched. `kernel/persist.c`'s call sites needed no logic changes — they only reference the named `PERSIST_*_LBA` constants, not hardcoded offsets.

**Disclosed trade-off, not a silent side effect.** `ROWSTORE_MAX_PAGES`/`VECSTORE_MAX_PAGES` (and the underlying 10 GiB disk image) were deliberately *not* grown alongside `PARTITION_MAX`, so each partition's guaranteed physical storage sub-range (Storage Isolation Phase 3, §17) now divides the same disk 256 ways instead of 16: rowstore per-partition capacity drops from 64 MiB to 4 MiB, vecstore from 16 MiB to 1 MiB. Both still divide evenly — no unreachable-page bug — and this is documented directly in `kernel/rowstore.h`/`kernel/vecstore.h`, not silently accepted. Raising the disk image size itself is a separate, larger decision left for whenever real per-tenant storage capacity requirements are known.

**RAM footprint, also a deliberate input, not a silent side effect.** Growing `TCP_MAX_CONNS` from 24 to 512 grows `tcp_conns[]` (each carrying a 32 KiB `rbuf[]`) and `net/http.c`'s `http_conns[TCP_MAX_CONNS]` (each 64 KiB) by roughly 46–47 MiB combined — a real, named cost of the new connection ceiling, not something that happened by accident.

**Verification.** `tests/tenant_host_test.c` Scenario H's `CHECK(rc == 2, ...)` capacity-exhaustion assertion was checked by hand against the exact loop bounds in `kernel/tenant.c` and `kernel/partition.c`: `partition_create()` reserves slot 0 for `PARTITION_SYSTEM` (`for (int i = 1; i < PARTITION_MAX; i++)`), so it still has exactly one fewer usable slot (255) than `TENANT_MAX`/`DATABASE_MAX` (256) even after all three were raised to the same nominal value — `partition_create()` remains the tighter, correctly-reported ceiling, and only the test's explanatory comment needed updating, not its logic or expected return code. Full regression: `tests/run_all.sh`, 56/56 host tests passing, zero regressions (run in four batches of 14 in this sandbox, no functional difference from a single pass).

**Status:** §4 and §5 item 9 / §7 item 8 (deliberate capacity sizing) are closed.
