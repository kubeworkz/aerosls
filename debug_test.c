/*
 * debug_test.c — leftover scratch file from Phase 23 verification (used to
 * bisect the row_journal.c null-termination bug documented in the
 * roadmap's §10 findings addendum). Not part of the build (not referenced
 * by the Makefile), not a real test suite, safe to ignore/delete.
 */
